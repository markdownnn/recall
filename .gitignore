node_modules/
dist/
dist-ext/
.vite/
test-results/
playwright-report/
recall-extension.zip
.DS_Store
src/.DS_Store
# The granite embedding model is COMMITTED under public/models/granite/ (plain git blobs;
# git-lfs was unavailable in the build env). The old e5 model is removed; ignore any stale
# local copy so it can't be re-committed.
public/models/Xenova/
.claude/worktrees/

# eval harness
eval/.cache/
eval/last-scorecard.json
